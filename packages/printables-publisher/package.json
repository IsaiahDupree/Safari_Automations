{
  "name": "@safari-automation/printables-publisher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Loopback-only, draft-first Printables release API using the managed Safari agent lane",
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "tsx src/server.ts",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.23.1",
    "typescript": "^5.3.3",
    "vitest": "^1.1.3"
  }
}
