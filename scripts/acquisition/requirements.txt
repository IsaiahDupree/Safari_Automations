# acquisition module dependencies
# Core Python stdlib is used for HTTP (urllib.request) to match project patterns.
# These are only needed for the agent-specific features.

anthropic>=0.40.0          # Claude API for ICP scoring + message generation
httpx>=0.27.0              # Async HTTP client (for entity resolution, email)
pydantic>=2.5.0            # Data validation and settings
python-dotenv>=1.0.0       # .env loading

# Email (Agent 08)
resend>=2.0.0              # Transactional email API
dnspython>=2.4.0           # DNS resolution for MX record checks
pyjwt>=2.8.0               # JWT tokens for unsubscribe links

# API server (Agent 07)
fastapi>=0.109.0           # REST API for orchestrator
uvicorn>=0.27.0            # ASGI server

# Scheduling
apscheduler>=3.10.0        # Cron-like scheduling for daily agent runs

# Testing
pytest>=7.4.0              # Testing framework
pytest-asyncio>=0.21.0     # Async test support
