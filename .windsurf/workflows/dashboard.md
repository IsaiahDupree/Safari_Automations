---
description: Show the Safari Automation dashboard with service status
---

// turbo
1. Run the dashboard CLI to see all service status:
```bash
npx tsx scripts/dashboard.ts
```

For live updating dashboard, use:
```bash
npx tsx scripts/dashboard.ts --watch
```
